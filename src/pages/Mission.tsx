import React from 'react';
import MissionSection from '@/components/MissionSection';

const Mission = () => {
    return (
        <div className="pt-20">
            <MissionSection />
        </div>
    );
};

export default Mission;
